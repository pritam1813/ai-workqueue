{
  "name": "ai-workqueue",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun services/api_gateway.ts",
    "db:generate": "bun --bun drizzle-kit generate",
    "db:migrate": "bun --bun drizzle-kit migrate",
    "db:push": "bun --bun drizzle-kit push"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^1.0.0-rc.4",
    "tsx": "^4.23.1"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "drizzle-orm": "^1.0.0-rc.4",
    "zod": "^4.4.3"
  }
}
